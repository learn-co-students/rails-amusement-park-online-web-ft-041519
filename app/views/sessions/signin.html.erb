<h1>This is the signin page<h1>

<%= form_for @user, url: sessions_path do |f| %>
    Name: <%= f.collection_select :name, User.all, :name, :name, prompt: true %><br>
    Password: <%= f.password_field :password %><br>
    <%= f.submit "Sign In" %><br>
<% end %>