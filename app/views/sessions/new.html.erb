<%= form_for @user, :url=> '/signin', :method=> "POST" do |f| %>
  Your name:  <%= f.collection_select :name, User.all, :name, :name, include_blank: false %><br />
  Your password: <input type = password password=password id=password >
  <%= f.submit "Sign In" %>
<% end %>
